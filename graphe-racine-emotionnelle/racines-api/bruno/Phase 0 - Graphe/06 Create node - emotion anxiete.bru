meta {
  name: 06 Create node - emotion anxiete
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/nodes
  body: json
  auth: none
}

body:json {
  {
    "type": "emotion",
    "label": "anxiété",
    "extra": { "valence": "négative" }
  }
}

script:post-response {
  bru.setVar("emotion2Id", res.body.id);
}
